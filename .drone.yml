kind: pipeline
type: docker
name: graphql-to

steps:
  - name: build
    image: maven
    commands:
      - mvn clean compile
    when:
      branch:
        - master
        - develop
        - feature/*
  - name: package
    image: maven
    commands:
      - mvn package
    when:
      branch:
        - master
        - develop
        - feature/*
